/ funcion de suma total
const { ayuda } = require('./helper.js')
const suma = (ns) =>{
	
	   
    acumulado += ns[i];
  }

  return acumulado
} 
//hola mundo estoy aprendiendo vim x
const unoAlCinco = [1, 2, 3, 4, 5];
// multiplicar * 2
const multiplicados = unoAlCinco.map(x => x * 2)
// a pares
const parejas = unoAlCinco.map(x => [x, x])

const mascotas = [

  { nombre: 'Puchini', edad: 12, tipo: 'perro' },
  { nombre: 'Chanchito robot', edad: 3, tipo: 'perro' },
  { nombre: 'Pelusa', edad: 12, tipo: 'gato' },
  { nombre: 'Pulga', edad: 10, tipo: 'perro' },
]
// edad promedio
const edades = mascotas.map(x => x.edad)
const resultado = suma(edades)
console.log(resultado / edades.length);
